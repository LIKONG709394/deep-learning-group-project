# deep-learning-group-project

Application code lives in **`classroom_blackboard_analytics/`**. See [classroom_blackboard_analytics/README.md](classroom_blackboard_analytics/README.md) for setup and usage.

**Branch `classroom-blackboard-analytics`**: nested layout above. **`main`**: may use a flat layout at repository root; check that branch’s tree if you clone `main` only.
