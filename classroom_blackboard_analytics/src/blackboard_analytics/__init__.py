# Frame + clip → board OCR, clarity, speech, how well they match, PDF.

from blackboard_analytics.model_cache import ensure_project_model_cache_dirs

__version__ = "0.1.0"

ensure_project_model_cache_dirs()
